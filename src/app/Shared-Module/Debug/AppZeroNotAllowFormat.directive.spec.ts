/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { AppZeroNotAllowFormatDirective } from '../Directives/AppZeroNotAllowFormat.directive';

describe('Directive: AppZeroNotAllowFormat', () => {
  it('should create an instance', () => {
    const directive = new AppZeroNotAllowFormatDirective();
    expect(directive).toBeTruthy();
  });
});
