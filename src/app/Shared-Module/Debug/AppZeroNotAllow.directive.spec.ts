/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { AppZeroNotAllowDirective } from '../Directives/AppZeroNotAllow.directive';

describe('Directive: AppZeroNotAllow', () => {
  it('should create an instance', () => {
    const directive = new AppZeroNotAllowDirective();
    expect(directive).toBeTruthy();
  });
});
